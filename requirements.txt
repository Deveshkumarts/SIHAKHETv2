# Core Deep Learning & Computer Vision
ultralytics>=8.3.0
torch>=2.1.0
torchvision>=0.16.0
opencv-python>=4.8.0
numpy>=1.24.0
matplotlib>=3.7.0
pandas>=2.0.0
Pillow>=10.0.0
PyYAML>=6.0.0
tqdm>=4.65.0

# Model Export & Optimization
onnx>=1.15.0
onnxruntime>=1.16.0

# Image & Data Augmentation utilities
scipy>=1.10.0
seaborn>=0.12.0

# Web UI & REST API Services (SIH26057)
streamlit>=1.30.0
fastapi>=0.100.0
uvicorn>=0.23.0
plotly>=5.18.0

# Geospatial & Hydrographic Survey (WGS-84 / IHO S-44)
pyproj>=3.6.0
shapely>=2.0.0

# MarineGuard v2 pipeline (YOLO11-Seg + Conv-AE + LSTM)
scikit-learn>=1.3.0        # ROC-AUC / PR-AUC, fusion model
scikit-image>=0.21.0       # SSIM for the autoencoder evaluation
onnx>=1.15.0               # model export (edge deployment)
onnxruntime>=1.16.0        # ONNX / TensorRT execution provider
httpx>=0.24.0              # FastAPI TestClient (tests)
pytest>=7.4.0
# psycopg2-binary>=2.9     # OPTIONAL: PostgreSQL/PostGIS backend (falls back to SQLite when absent)
